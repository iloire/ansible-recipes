# MacBook Pro - LLM Agent Machine
# Minimal setup: shared shell config, git, node, chrome, claude
# No dev tools (neovim, vscode, cursor, docker) or personal config (myconfig, crontab)

- name: macOS Agent Setup
  hosts: osx_agent
  gather_facts: true
  roles:
    - macos_cleanup
    - macos_server
    - dotfiles
    - git_config
    - tmux
    - node
    - npm_packages
    - hosts_file
    - claude
    - homebrew
    - mapcapsescape
    - osx_config
    - sysinfo
