apt_minimum_packages:
  - arp-scan
  - awscli
  - curl
  - dconf-cli
  - dconf-editor
  - exiv2
  - ffmpeg
  - fzf
  - git
  - git-crypt
  - htop
  - iperf
  - mailutils
  - mc
  - net-tools
  - nmap
  - openssh-client
  - openssh-server
  - pv
  - python3
  - python3-pip
  - ripgrep
  - smartmontools
  - sysstat
  - tmux
  - tmuxinator
  - tree
  - vim
  - webp
  - x11-xkb-utils
  - zsh

# libfuse2 is needed by AppImages. Package was renamed `libfuse2` →
# `libfuse2t64` in the 24.04 t64 transition; pick the right name based
# on the Ubuntu major version.
apt_minimum_libfuse:
  - "{{ 'libfuse2t64' if ansible_distribution_major_version | int >= 24 else 'libfuse2' }}"

apt_packages:
  - alacritty
  - aspell-es
  - awesome
  - bat
  - blueman
  - btop
  - caffeine
  - calibre
  - cmatrix
  - cmus
  - code
  - conky-std
  - du-dust
  - fd-find
  - ffmpeg
  - filezilla
  - fish
  - flameshot
  - flatpak
  - gh
  - git-delta
  - gparted
  - gnome-shell-extension-manager
  - gnome-software-plugin-flatpak
  - gnome-tweaks
  - hardinfo
  - imagemagick
  - jq
  - kazam
  - libreoffice
  - mysql-client
  - nautilus-image-converter
  - nvidia-driver-525
  - pavucontrol
  - postgresql-client
  - stripe
  - telegram
  - terminator
  - torbrowser-launcher
  - virtualbox
  - vlc
  - wgrib2
  - wmctrl
  - workrave
  - yara
  - yt-dlp

apt_remove_packages:
  - thunderbird

apt_ppas:
  - "ppa:atareao/telegram"
  - "ppa:aslatter/ppa"

# Third-party APT repos that aren't on Launchpad and aren't already
# handled by a dedicated role. Each entry installs a GPG key into
# /etc/apt/keyrings/<name>.gpg and adds a deb line in
# /etc/apt/sources.list.d/<name>.list referencing it via signed-by=.
#
# Repos handled elsewhere (do NOT add them here):
#   1password         → roles/onepassword
#   brave-browser     → roles/brave
#   docker            → roles/docker
#   github-desktop    → roles/github_desktop
#   google-chrome     → roles/google_chrome
#   hashicorp/vagrant → roles/vagrant
apt_third_party_repos:
  - name: nvidia-container-toolkit
    key_url: https://nvidia.github.io/libnvidia-container/gpgkey
    repo: "deb [signed-by=/etc/apt/keyrings/nvidia-container-toolkit.gpg] https://nvidia.github.io/libnvidia-container/stable/deb/$(ARCH) /"
  - name: virtualbox
    key_url: https://www.virtualbox.org/download/oracle_vbox_2016.asc
    repo: "deb [arch=amd64 signed-by=/etc/apt/keyrings/virtualbox.gpg] http://download.virtualbox.org/virtualbox/debian {{ ansible_distribution_release }} contrib"
  - name: vscode
    key_url: https://packages.microsoft.com/keys/microsoft.asc
    repo: "deb [arch=amd64,arm64,armhf signed-by=/etc/apt/keyrings/vscode.gpg] https://packages.microsoft.com/repos/code stable main"

snap_packages:
  - dart-sass
  - discord
  - dust
  - fbreader
  - foliate
  - gimp
  - icloud-notes-linux-client
  - ngrok
  - spotify
  - todoist
  - zoom-client

snap_classic_packages:
  - aws-cli
  - blender
  - google-cloud-sdk
  - obsidian

flatpak_packages:
  - org.deskflow.deskflow

nerd_fonts_version: "v3.4.0"
nerd_fonts:
  - { name: Hack, check: "Hack*" }
  - { name: DroidSansMono, check: "Droid*" }
  - { name: JetBrainsMono, check: "JetBrains*" }
  - { name: UbuntuMono, check: "Ubuntu*" }
  - { name: Ubuntu, check: "Ubuntu*" }
