- name: install 1password
  ansible.builtin.shell:
    cmd: |
      curl -Lo /tmp/1password.deb "https://downloads.1password.com/linux/debian/amd64/stable/1password-latest.deb" && apt install -y /tmp/1password.deb && rm /tmp/1password.deb
    creates: /usr/bin/1password
  become: true
